image: cirrusci/flutter:latest

before_script:
  - flutter --version 2.10.0  # Specify the Flutter version directly
  - flutter config --enable-web  # Enable web support if needed
  - flutter doctor
  - flutter pub get

stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - flutter build apk --release
  artifacts:
    paths:
      - build/app/outputs/flutter-apk/app-release.apk

unit-test-job:
  stage: test
  script:
    - flutter test
  allow_failure: true

lint-test-job:
  stage: test
  script:
    - flutter analyze
  allow_failure: true

deploy-job:
  stage: deploy
  script:
    - echo "Deploying application..."
    - echo "Application successfully deployed."
  environment:
    name: production
  dependencies:
    - build-job
  artifacts:
    paths:
      - build/app/outputs/flutter-apk/app-release.apk
